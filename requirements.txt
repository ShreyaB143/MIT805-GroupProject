# MIT 805 Group Project — pinned per project-plan-and-guide.md
# venv + pip (not conda). PySpark 4.2.0 does not yet fully support pandas >= 3.0.

pyspark==4.2.0
pandas>=2.2,<3.0
numpy>=1.26
pyarrow>=15.0        # fast Parquet read/write for PySpark + pandas
jupyter>=1.0
ipykernel>=6.29
matplotlib>=3.8
seaborn>=0.13
findspark>=2.0.1     # convenience for locating Spark from a notebook/venv
python-dotenv>=1.0   # for SPARK_HOME / JAVA_HOME etc. via a .env file
pytest>=8.0          # for any pipeline unit tests
